<form theme="light">
  <label>Overview</label>
  <search id="basesearch">
    <query>index=_internal sourcetype="scheduler" status="success" app="central_security_app_for_splunk"
| where NOT (alert_action!="" OR (result_count&gt;0 AND suppressed=0))
| stats count as execution_count, latest(scheduled_time) as last_execution, avg(run_time) as avg_runtime by savedsearch_name
| append [| search index=_internal sourcetype="scheduler" status="success" app="central_security_app_for_splunk"
| where (alert_action!="" OR (result_count&gt;0 AND suppressed=0))
| stats count as alert_count, latest(scheduled_time) as last_alert_fired_time, latest(result_count) as last_result_count, avg(run_time) as avg_runtime by savedsearch_name]
| stats avg(avg_runtime) as avg_runtime, first(*) as * by savedsearch_name
| eval avg_runtime=round(avg_runtime, 2), last_alert_fired_time=strftime(last_alert_fired_time, "%F %T"), last_execution=strftime(last_execution, "%F %T")
| append [| rest /servicesNS/-/central_security_app_for_splunk/saved/searches count=0 splunk_server=local | search "eai:acl.app"="central_security_app_for_splunk" | eval encodedtitle=title | replace " " with "%20", "," with "%2C", "'" with "%27" in encodedtitle | rename title as savedsearch_name, "alert.severity" as severity, "dispatch.earliest_time" as earliest_time, "dispatch.latest_time" as latest_time, "eai:acl.app" as app | fields savedsearch_name, encodedtitle, disabled, severity, cron_schedule, description, earliest_time, latest_time, app, is_scheduled, next_scheduled_time, triggered_alert_count]
| stats first(*) as * by savedsearch_name
| eval alert_count=if(isnull(alert_count), 0, alert_count)
| table savedsearch_name, description, app, disabled, is_scheduled, alert_count, last_alert_fired_time, last_result_count, execution_count, last_execution, avg_runtime, cron_schedule, earliest_time, latest_time, next_scheduled_time, severity
| search disabled=0 is_scheduled=1 
| search NOT savedsearch_name IN ("Windows - Hosts Missing Update", "Windows - Hosts Lookup Gen", "Ransomware - Calculate UpperBound for Spike in File Writes")
| table savedsearch_name, alert_count</query>
    <earliest>$timeRange.earliest$</earliest>
    <latest>$timeRange.latest$</latest>
    <sampleRatio>1</sampleRatio>
  </search>
  <fieldset submitButton="false">
    <input type="time" token="timeRange">
      <label>Time Range</label>
      <default>
        <earliest>-24h@h</earliest>
        <latest>now</latest>
      </default>
    </input>
  </fieldset>
  <row>
    <panel>
      <title>Network Compromise</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("Network Compromise")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
    <panel>
      <title>Endpoint Compromise</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("Endpoint Compromise")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
  </row>
  <row>
    <panel>
      <title>Ransomware</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("Ransomware")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
    <panel>
      <title>Scanning</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("Scanning")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
  </row>
  <row>
    <panel>
      <title>Windows &amp; AD</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("Windows|AD")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
    <panel>
      <title>O365</title>
      <table>
        <search base="basesearch">
          <query>| `cs_filter_savedsearches("O365")`</query>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
  </row>
  <row>
    <panel>
      <title>Monthly Alerts</title>
      <input type="time" token="timeRange_long">
        <label>Time Range</label>
        <default>
          <earliest>-30d@d</earliest>
          <latest>now</latest>
        </default>
      </input>
      <table>
        <search>
          <query>index=_internal sourcetype="scheduler" status="success" app="central_security_app_for_splunk" savedsearch_name IN ("Windows - Hosts Missing Update")
| where NOT (alert_action!="" OR (result_count&gt;0 AND suppressed=0))
| stats count as execution_count, latest(scheduled_time) as last_execution, avg(run_time) as avg_runtime by savedsearch_name
| append [| search index=_internal sourcetype="scheduler" status="success" app="central_security_app_for_splunk"  savedsearch_name IN ("Windows - Hosts Missing Update")
| where (alert_action!="" OR (result_count&gt;0 AND suppressed=0))
| stats count as alert_count, latest(scheduled_time) as last_alert_fired_time, latest(result_count) as last_result_count, avg(run_time) as avg_runtime by savedsearch_name]
| stats avg(avg_runtime) as avg_runtime, first(*) as * by savedsearch_name
| eval avg_runtime=round(avg_runtime, 2), last_alert_fired_time=strftime(last_alert_fired_time, "%F %T"), last_execution=strftime(last_execution, "%F %T")
| append [| rest /servicesNS/-/central_security_app_for_splunk/saved/searches count=0 splunk_server=local | search "eai:acl.app"="central_security_app_for_splunk" | eval encodedtitle=title | replace " " with "%20", "," with "%2C", "'" with "%27" in encodedtitle | rename title as savedsearch_name, "alert.severity" as severity, "dispatch.earliest_time" as earliest_time, "dispatch.latest_time" as latest_time, "eai:acl.app" as app | fields savedsearch_name, encodedtitle, disabled, severity, cron_schedule, description, earliest_time, latest_time, app, is_scheduled, next_scheduled_time, triggered_alert_count]
| stats first(*) as * by savedsearch_name
| search savedsearch_name IN ("Windows - Hosts Missing Update")
| eval alert_count=if(isnull(alert_count), 0, alert_count)
| table savedsearch_name, description, app, disabled, is_scheduled, alert_count, last_alert_fired_time, last_result_count, execution_count, last_execution, avg_runtime, cron_schedule, earliest_time, latest_time, next_scheduled_time, severity
| table savedsearch_name, disabled, is_scheduled, alert_count, last_alert_fired_time, last_result_count, description, execution_count, last_execution, avg_runtime, cron_schedule
| search disabled=0 is_scheduled=1
| table savedsearch_name, alert_count</query>
          <earliest>$timeRange_long.earliest$</earliest>
          <latest>$timeRange_long.latest$</latest>
        </search>
        <option name="count">10</option>
        <option name="drilldown">none</option>
        <option name="refresh.display">progressbar</option>
        <format type="color" field="disabled">
          <colorPalette type="map">{"1":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="is_scheduled">
          <colorPalette type="map">{"0":#F7BC38}</colorPalette>
        </format>
        <format type="color" field="alert_count">
          <colorPalette type="list">[#03991a,#a3030b]</colorPalette>
          <scale type="threshold">1</scale>
        </format>
      </table>
    </panel>
  </row>
</form>